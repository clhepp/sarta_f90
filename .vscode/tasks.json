{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gfbuild",
            "command": ["cd ./src && make airs_nn && make clean && cd .."]
        }
    ]
}
